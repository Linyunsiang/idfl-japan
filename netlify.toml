# redeploy trigger: pick up access-control env vars (CUSTOMER/STAFF/SESSION_SECRET)
[build]
  publish = "."
[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"

[[redirects]]
  from = "/admin"
  to = "/admin.html"
  status = 200
